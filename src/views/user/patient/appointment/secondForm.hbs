<div class="row register-form">
	<div class="col-md-8 offset-md-2">
		<form class="custom-form" action="/user/selectDoctor" method="POST">
			<h1>Agenda tu cita</h1>
			<div class="row form-group">
				<div class="col-sm-4 label-column">
					<label class="col-form-label" for="dropdown-departament">Área de
						especialidad
					</label>
				</div>
				<div class="col-sm-4 input-column">
					<div class="dropdown">
						<select
							name="departament"
							id="dropdown-departament"
							class="btn btn-light dropdown-toggle"
						>
							{{#if dep}}
								<option class="dropdown-item" value="{{dep}}" selected>
									{{formatOption dep}}
								</option>
							{{/if}}
						</select>
					</div>
				</div>
			</div>
			<div class="row form-group">
				<div class="col-sm-4 label-column">
					<label class="col-form-label" for="appointment_date">Fecha para la
						cita
					</label>
				</div>
				<div class="col-sm-6 input-column">
					<input
						class="form-control"
						name="date"
						type="date"
						id="appointment_date"
						required
					/>
				</div>
			</div>
			<div class="row form-group">
				<div class="col-sm-4 label-column">
					<label class="col-form-label" for="dropdown-doctor">Doctor
					</label>
				</div>
				<div class="col-sm-4 input-column">
					<div class="dropdown">
						<select
							name="doctor"
							id="dropdown-doctor"
							class="btn btn-light dropdown-toggle"
						>
							{{{createOptionsDoctors listDoctors}}}
						</select>
					</div>
				</div>
			</div>
			<button class="btn btn-light submit-button" onclick="saveLocalStorage()">
				Siguiente
			</button>
		</form>
	</div>
</div>
{{{getLocalStorage}}}